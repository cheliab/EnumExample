namespace EnumExample
{
    // Явное указание значений элементов
    
    public enum Operation
    {
        Add = 1,  // каждый следующий элемент увеличивается на 1
        Subtract, // 2
        Multiply, // 3
        Divide    // 4
    }
}